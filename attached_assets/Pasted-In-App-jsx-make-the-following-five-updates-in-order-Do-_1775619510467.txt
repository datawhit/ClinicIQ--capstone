In App.jsx make the following five updates in order. Do not change any existing logic, API calls, or data structures unless explicitly stated.
1. GRADUATION GOALS THREE SECTIONS
In the Requirements tab where customGoals.filter(g => g.visible) is mapped to render goal cards, replace the flat list with three labeled sections in this order: Complete (completed >= required), Needs Cases (completed > 0 and < required), Not Started (completed === 0). Add const [showAllGoals, setShowAllGoals] = useState(false). When showAllGoals is false hide the Not Started section entirely. Add a small toggle button below the Needs Cases section that reads "Show Not Started (N)" where N is the count of not started goals. When true show all three sections and change the button label to "Hide Not Started". Each section gets a small uppercase header in NYU.gray400 with the section name and count, for example "✓ COMPLETE (3)". Add marginTop: 24, marginBottom: 10 between sections. Keep all existing goal card UI identical, only group them.
2. SETTINGS MODAL TABS
Add const [settingsTab, setSettingsTab] = useState('profile'). Add a horizontal row of five pill buttons at the top of the settings modal content area below the header: Profile, Schedule, Rotations, Appearance, Activity. Active pill uses background: T.purple, color: 'white'. Inactive uses background: NYU.gray100, color: NYU.gray600. Use the existing .pill-btn className. Show only the matching content per tab. Profile tab: profile fields and graduation date and demo data block. Schedule tab: clinic schedule only. Rotations tab: rotations section only. Appearance tab: color themes, tab order, stat visibility. Activity tab: activity log only. The Save Changes and Cancel buttons at the bottom remain always visible regardless of active tab.
3. COLLAPSE PATIENT DETAIL SECTIONS
Add const [expandedSections, setExpandedSections] = useState(['visitHistory']). In the Patient Detail screen wrap each of these sections in a collapsible container: Treatment Phase, Specialty Referral, Lab and Pre-Auth, Notes, Patient Language, Paired Provider. Visit History starts expanded. All others start collapsed. Each section header shows the title on the left and a chevron on the right that rotates 90 degrees when open. Clicking the header toggles that section. Keep all existing content inside each section identical.
4. REMOVE STATS GRID
Remove the stats grid row (the five white cards showing Total Patients, Active, Needs Attention, Complete, Pre-Auth Alerts) from the main dashboard and from the Roster Panel. Do not remove the stats calculation logic, only remove the rendered grid UI elements. The same information is already shown in the Caseload Intelligence panel.
5. SIMPLIFY HAMBURGER MENU
In the hamburger dropdown menu, remove these items: Patient Roster, Graduation Goals, Notebook, Calendar. These duplicate the tab navigation. Keep only: Paired Provider View, Urgent Patients, Add Patient, Import Roster (if it exists), Export Roster, Settings, Sign Out. This makes the menu actions-only.