I can see exactly what's happening. There are actually two separate alias problems here:

**Problem 1 — The display name (hero card heading) is showing `Chart-2298475`**
The alias field is being set to `Chart-2298475` instead of `P-2026-NNN`. This means somewhere in the import flow, the alias is being built by concatenating "Chart-" with the chart number instead of calling `generateAlias`.

**Problem 2 — The internal ID is showing `PT-MNPGW9GA-01d`**
That is a randomly generated string, not the standard `PT-001` format from your `generateId` function. This means `generateId` is also not being called correctly during import — it's generating a random ID instead.

Both issues are in the `buildImportPreview` function or the server-side import handler. Here's the targeted fix prompt:

---

> In `App.jsx` and `server.js`, fix the alias and ID generation for imported patients.
>
> In `App.jsx`, in the `buildImportPreview` function, make these two changes. First, change the `id` generation to use `generateId` passing the current patients array plus already-processed rows, specifically: `id: \`PT-${String(patients.length + index + 1).padStart(3, '0')}\`` — do not use any random string generator. Second, change the `alias` generation to use `generateAlias` correctly: `alias: generateAlias(patients.length + index)` which produces the `P-YYYY-NNN` format. Do not concatenate "Chart-" with the chart number anywhere in the import flow.
>
> In `server.js`, in the `/api/patients/import` endpoint, when saving each patient, do not override or regenerate the alias or id that comes from the frontend. Trust the alias and id generated by `buildImportPreview` on the client side. If the incoming patient object has a valid alias starting with `P-` and a valid id starting with `PT-`, save them as-is. Only regenerate if those fields are missing or empty.
>
> Also find where the alias field is being displayed as the patient name in the hero card of the Patient Detail screen. Confirm it is reading `patient.alias` and not `patient.id` or `patient.chartNumber`. If any fallback exists that reads `patient.chartNumber` when alias is empty, replace it with a call to `generateAlias` using the patient's index in the patients array instead.